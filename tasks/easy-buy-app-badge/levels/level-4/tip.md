LLM вполне может часть стилей описать в `styles.ts`, а часть оставить в файле `Component.tsx`.

Почему? Потому что! Для LLM нет разницы между хорошей полностью выполненной работой и работой, сделанной частично и кое-как.

Это ваша задача — следить, чтоб она не сачковала.
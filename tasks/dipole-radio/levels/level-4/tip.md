Этот компонент отличается от предыдущего (`dipole-checkbox`) только радиомаркером вместо маркера чекбокса. Кругом вместо квадрата.

Оба компонента — из одного проекта. Разумно будет сверстать их максимально единообразно.

Постарайтесь сделать так, чтоб компонент `dipole-checkbox` отличался от компонента `dipole-checkbox` *ровно одним стилем*.
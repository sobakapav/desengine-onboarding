На уровне 2 мы развиваем уже существующую реализацию и учимся обобщать её через параметризацию.
В работу входят `base.png` и `variants.png`: вторая картинка должна получаться развитием первой, а не отдельной независимой вёрсткой.
На этом уровне пользователь может править `Component.tsx`, `Component.stories.ts` и `styles.ts`.
